services:
  dev:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true   # mantiene STDIN abierto
    tty: true          # asigna una terminal interactiva